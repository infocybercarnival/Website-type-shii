# Only used by `pnpm dev` (frontend running standalone on :3000 against a
# separately-running backend on :5000). Not used in `pnpm build` — the
# production static export defaults to same-origin relative API calls,
# because Flask serves both the site and the API from one process/port.
NEXT_PUBLIC_API_URL=http://localhost:5000
